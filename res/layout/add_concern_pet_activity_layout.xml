<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".petcommunity.AddConcernPetActivity" >

    <TextView
        android:id="@+id/add_concern_pet_numberLabel_textView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="@string/add_concern_pet_numberLabel_textView_text"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <EditText
        android:id="@+id/add_concern_pet_number_textEdit"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/add_concern_pet_numberLabel_textView"
        android:layout_marginTop="10dp"
        android:background="@drawable/common_edittext_bg"
        android:ellipsize="end"
        android:inputType="number" >

        <requestFocus />
    </EditText>

</RelativeLayout>
